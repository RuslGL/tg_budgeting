services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped

  dashboard:
    build: ./dashboard
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - dashboard_data:/data
    restart: unless-stopped

volumes:
  dashboard_data:
